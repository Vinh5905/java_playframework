play.http.secret.key = "changeme-todo-sync-project-secret"

play.filters.hosts {
  allowed = ["localhost", "localhost:9000", "127.0.0.1", "127.0.0.1:9000"]
}

# Tắt CSRF để test dễ hơn với curl (CHỈ trong dev/learning)
play.filters.disabled += "play.filters.csrf.CSRFFilter"
